import { searchPanel } from './searchPanel.js';
import { loadMarkdown } from './markdownLoader.js';

export const subMenu = {
        // ...as in your code, but with imports and exports,
        // replace direct references to global 'searchPanel' with imported 'searchPanel'
};